<%= form_for(@contact) do |f| %>
  <% if @contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% @contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description, 'Message' %><br>
    <%= f.text_area :description, :size => "60x20" %>
  </div>
  <div class="field">
    <%= f.label :email, 'Your Email' %><br>
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit 'Contact us :)' %>
  </div>
<% end %>
